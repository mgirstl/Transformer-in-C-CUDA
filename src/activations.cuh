/**
 * @file activations.cuh
 * @brief This file includes several headers which define classes and functions
 * within the `activations` namespace.
 *
 * Please refer to the individual header files for detailed documentation on
 * each class and function.
 */

#pragma once

#include "activations/activation.cuh"
#include "activations/identity.cuh"
#include "activations/relu.cuh"
#include "activations/softmax.cuh"
